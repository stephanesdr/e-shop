	<div class="container p-5">


<<<<<<< HEAD
		<div class="row ml-3">
		
			<div class="col-12 ml-5">
						<%= link_to new_administration_item_path do %>
				<p class="btn btn-info mt-4"><i class="fas fa-plus fa-3x"></i></p>
				<% end %>
			 <%= render "table_row" %>
=======
		<div class="row">
			<div class="col-12">

				<div class="card">
					<div class="table-responsive">
						<table class="table table-striped table-hover">
							<thead>
								<tr>
									<th>ID</th>
								  <th>Produit</th>
									<th>Prix de départ</th>
                  <th>Reduction</th>
									<th>Remise</th>
                  <th>Nouveau Prix</th>
									<th>Description</th>
									<th>Categories</th>
									<th>Edition</th>
                  <th>Supression</th>
								</tr>
							</thead>
							<tbody>
                <%@items.order("id ASC").each do |item| %>
                <tr>
									<td><%= item.id%></td>
                  <td><%= link_to(item.name, home_item_path(item.id)) %></td>
                  <td><%= item.price%> €</td>
                  <td><%= item.discount ? "Oui" : "Non" %></td>
                  <td><%= item.percentage_discount%> %</td>
                  <td><%= item.reduced_price%> €</td>
                  <td><%= item.description%></td>
									<td><% item.categories.each do|cat| %>
											<%=cat.name %>
											<%end%>
									</td>
									<td><%= link_to "EDITER" , edit_administration_item_path(item.id) %></td>
									<td><%= link_to "DELETE", administration_item_path(item.id), :method => :delete,  data: { confirm: 'Are you sure you want to delete this item from your stock ?' } %></td>
                </tr>
                <% end%>
							</tbody>
						</table>

					</div>
				</div>
        <br>
        <%= link_to "Ajouter une item",  new_administration_item_path%>
>>>>>>> master
			</div>
		</div>

	</div>
