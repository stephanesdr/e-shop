
<% if @item.picture.attached? %>
  <%= image_tag @item.picture, alt: 'picture', class: 'picture' %>
<% else %>
  <img class="card-img-top" src="<%= image_url(@item.image_url) %>" alt="<%= @item.name %>" size="250" >
<% end %>
