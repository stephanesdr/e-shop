VOS COMMANDES <br>
<table class="table table-striped table-hover">
    <thead>
        <tr>
            <th>ID</th>
            <th>Date</th>
            <th>Nombres d'articles</th>
            <th>Prix</th>
            <th>Status</th>
        </tr>
    </thead>
    <tbody>
        <%@orders.order('created_at ASC').each do |order| %>
        <tr>
            <td>
                <%= link_to home_order_path(order.id) do %>
                Commande #<%= order.id %>
                <%end%>
            </td>
            <td><%= order.created_at.asctime%></td>
            <td><%=order.items.count %></td>
            <td><%=order.items.sum("price")%> €</td>
            <td><%=order.status %></td>
        </tr>
        <%end%>
    </tbody>


</table>