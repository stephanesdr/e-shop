<div class="sticky-top sticky-top-blocknav">
    <div class="list-group">
        <% @categories.each do |category| %>
        <div class="list-group-item">
            <%= link_to(home_category_items_path(category.id)) do %>
            <%= category.name %>
            <% end %>
        </div>
        <% end %>
    </div>
</div>