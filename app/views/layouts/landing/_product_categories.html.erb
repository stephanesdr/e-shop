<div class="pt-2"></div>
<div class=" shadow-sm scrollbar__category ">
    <div class="list-group">
        <% @categories.alphabet.each do |category| %>
        <div class="list-group-item">
            <%= link_to(home_category_path(category.id)) do %>
            <%= category.name %>
            <% end %>
        </div>
        <% end %>
    </div>
</div>
